# PostgreSQL Database Configuration
# Format: postgresql://username@host:port/database_name
DATABASE_URL=postgresql://7racker@localhost:5432/9layer_dev
DOWNLOAD_DIR="/Volumes/2TB/coding tools/9layer/music"

# Note: This assumes PostgreSQL is running locally on the default port (5432)
# with a database named '9layer_dev' and the default 'postgres' user with no password
