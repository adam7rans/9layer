# PostgreSQL Database Configuration
# Format: postgresql://username@host:port/database_name
DATABASE_URL=postgresql://postgres@localhost:5432/music_player
MUSIC_DOWNLOAD_DIR="/Volumes/2TB/coding tools/9layer/music"

# Note: This assumes PostgreSQL is running locally on the default port (5432)
# with a database named 'music_player' and the default 'postgres' user with no password
